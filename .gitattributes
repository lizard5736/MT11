# Normalise line endings in the repository; Windows checkouts get CRLF as usual.
* text=auto

# Scripts run on Linux and must keep LF.
*.sh text eol=lf
*.py text eol=lf

# Binary assets.
*.png binary
*.ico binary
